# Demo

Stage 0 placeholder. From Stage 7 this contains the judge-facing demo runbook:

- Scenario A — extraction drift recovery loop.
- Scenario B — semantic drift + compatibility violation.

Three consecutive full passes from reset state are required.
