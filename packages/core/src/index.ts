/**
 * @modelcontract/core — framework-independent domain rules.
 *
 * Stage 0: package boundary only.
 * Stage 1+: ModelContract, DriftType, normalizers, semantic hash,
 *           extraction validation, drift classification, healing
 *           verification, compatibility evaluation.
 */

export {};
