/**
 * @modelcontract/db — persistence (Prisma + PostgreSQL).
 *
 * Stage 0: package boundary only.
 * Stage 2+: Prisma schema (Provider, Model, Contract, Observation,
 *           CollectorVersion, DriftEvent, HealAttempt, CompatibilityCheck,
 *           DemoState) and shared client.
 */

export {};
